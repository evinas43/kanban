<Window x:Class="Kanban.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kanban"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
       
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#FF0D1B2A" Offset="0"/>
                <GradientStop Color="#FF1B2B3A" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
   
        <StackPanel Margin="40">
                                  
            <TextBlock Text="Kanban" 
                       Foreground="White"
                       FontSize="36"
                       FontWeight="Bold"/>
            <TextBlock Text="Gestor de tasques"
                       Foreground="#88FFFFFF"
                       FontSize="16"
                       Margin="0,4,0,20"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>


                <Border Grid.Column="0" Margin="10" CornerRadius="12" Background="#112233">
                    <StackPanel>
                        <Border Background="#2962FF" CornerRadius="8" Padding="10" Margin="8">
                            <TextBlock Text="To Do" Foreground="White" FontWeight="Bold" FontSize="16"/>
                        </Border>
                        <ListView x:Name="toDoList" Margin="8" Background="#2A2F3A" BorderThickness="0" Foreground="White"  MouseDoubleClick="TaskListView_MouseDoubleClick" Padding="6" >
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="6" Padding="6" HorizontalAlignment="Stretch" Width="400">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition/>
                                                <RowDefinition/>
                                                <RowDefinition/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="{Binding titul}" FontWeight="Bold" Foreground="White"/>
                                            <TextBlock Text="{Binding Descripció}" FontSize="12" Foreground="#88FFFFFF" Grid.Row="1"/>
                                            <Border CornerRadius="4" Padding="4,2" HorizontalAlignment="Left" Margin="0,6,0,0" Grid.Row="2">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="Green"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding prioritat}" Value="mid">
                                                                <Setter Property="Background" Value="Orange"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding prioritat}" Value="high">
                                                                <Setter Property="Background" Value="Red"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding prioritat}" Foreground="White" FontSize="11"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- Doing -->
                <Border Grid.Column="1" Margin="10" CornerRadius="12" Background="#112233">
                    <StackPanel>
                        <Border Background="#FFA000" CornerRadius="8" Padding="10" Margin="8">
                            <TextBlock Text="Doing" Foreground="White" FontWeight="Bold" FontSize="16"/>
                        </Border>
                        <ListView x:Name="doingList" Margin="8" Background="#1E2A38" BorderThickness="0" Foreground="White" MouseDoubleClick="TaskListView_MouseDoubleClick" Padding="6">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#2A2F3A" CornerRadius="6" Padding="6" Margin="4">
                                        <StackPanel>
                                            <TextBlock Text="{Binding titul}" FontWeight="Bold" Foreground="White"/>
                                            <TextBlock Text="{Binding Descripció}" FontSize="12" Foreground="#88FFFFFF"/>
                                            <Border CornerRadius="4" Padding="4,2" HorizontalAlignment="Left" Margin="0,6,0,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="Green"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding prioritat}" Value="mid">
                                                                <Setter Property="Background" Value="Orange"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding prioritat}" Value="high">
                                                                <Setter Property="Background" Value="Red"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding prioritat}" Foreground="White" FontSize="11"/>
                                            </Border>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- To Review -->
                <Border Grid.Column="2" Margin="10" CornerRadius="12" Background="#112233">
                    <StackPanel>
                        <Border Background="#AA00FF" CornerRadius="8" Padding="10" Margin="8">
                            <TextBlock Text="To Review" Foreground="White" FontWeight="Bold" FontSize="16"/>
                        </Border>
                        <ListView x:Name="toReviewList" Margin="8" Background="#1E2A38" BorderThickness="0" Foreground="White" MouseDoubleClick="TaskListView_MouseDoubleClick" Padding="6">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#2A2F3A" CornerRadius="6" Padding="6" Margin="4">
                                        <StackPanel>
                                            <TextBlock Text="{Binding titul}" FontWeight="Bold" Foreground="White"/>
                                            <TextBlock Text="{Binding Descripció}" FontSize="12" Foreground="#88FFFFFF"/>
                                            <Border CornerRadius="4" Padding="4,2" HorizontalAlignment="Left" Margin="0,6,0,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="Green"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding prioritat}" Value="mid">
                                                                <Setter Property="Background" Value="Orange"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding prioritat}" Value="high">
                                                                <Setter Property="Background" Value="Red"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding prioritat}" Foreground="White" FontSize="11"/>
                                            </Border>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- Done -->
                <Border Grid.Column="3" Margin="10" CornerRadius="12" Background="#112233">
                    <StackPanel>
                        <Border Background="#00C853" CornerRadius="8" Padding="10" Margin="8">
                            <TextBlock Text="Done" Foreground="White" FontWeight="Bold" FontSize="16"/>
                        </Border>
                        <ListView x:Name="doneList" Margin="8" Background="#1E2A38" BorderThickness="0" Foreground="White" MouseDoubleClick="TaskListView_MouseDoubleClick" Padding="6">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#2A2F3A" CornerRadius="6" Padding="6" Margin="4">
                                        <StackPanel>
                                            <TextBlock Text="{Binding titul}" FontWeight="Bold" Foreground="White"/>
                                            <TextBlock Text="{Binding Descripció}" FontSize="12" Foreground="#88FFFFFF"/>
                                            <Border CornerRadius="4" Padding="4,2" HorizontalAlignment="Left" Margin="0,6,0,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="Green"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding prioritat}" Value="mid">
                                                                <Setter Property="Background" Value="Orange"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding prioritat}" Value="high">
                                                                <Setter Property="Background" Value="Red"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding prioritat}" Foreground="White" FontSize="11"/>
                                            </Border>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

            </Grid>
        </StackPanel>
    </Grid>
</Window>
