<Window x:Class="Kanban.UserForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Crear Usuario" Height="410" Width="400" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#FF0D1B2A" Offset="0"/>
                <GradientStop Color="#FF1B2B3A" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:" Foreground="White" Margin="0,20,0,0" Grid.Row="0"/>
        <TextBox x:Name="UsernameBox" Grid.Row="0" Margin="125,21,25,5" Width="250"/>

        <TextBlock Text="Nom:" Foreground="White"  Grid.Row="1"/>
        <TextBox x:Name="NomBox" Grid.Row="1" Margin="100,0,0,5" Width="250"/>

        <TextBlock Text="Cognom:" Foreground="White"  Grid.Row="2"/>
        <TextBox x:Name="CognomBox" Grid.Row="2" Margin="100,0,0,5" Width="250"/>

        <TextBlock Text="Password:"  Foreground="White" Grid.Row="3"/>
        <PasswordBox x:Name="PasswdBox" Grid.Row="3" Margin="100,0,0,5" Width="250"/>

        <CheckBox x:Name="IsAdminCheck" Grid.Row="4" Content="Administrador" Foreground="White" Margin="0,5,0,0"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="5" Margin="0,135,0,47">
            <Button x:Name="ActionButton" Content="➕ Crear" Width="120" Margin="5" Click="ActionButton_Click" Height="34"/>
            <Button Content="❌ Cancelar" Width="120" Margin="5" Click="Cancel_Click" Height="34"/>
        </StackPanel>
    </Grid>
</Window>
